import { ClockDirective } from './clock.directive';

describe('ClockDirective', () => {
  it('should create an instance', () => {
    const directive = new ClockDirective();
    expect(directive).toBeTruthy();
  });
});
