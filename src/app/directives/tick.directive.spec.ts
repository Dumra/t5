import { TickDirective } from './tick.directive';

describe('TickDirective', () => {
  it('should create an instance', () => {
    const directive = new TickDirective();
    expect(directive).toBeTruthy();
  });
});
